<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sorveteria.eradogelo.sorveteriaeradogelo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main -->
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Menu -->
        <activity android:name=".MenuEstoque" android:label="@string/labelMenuEstoque" android:parentActivityName=".Main">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>
        <activity android:name=".MenuPedido" android:label="@string/labelMenuPedido" android:parentActivityName=".Main">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>
        <activity android:name=".MenuFornecedor" android:label="@string/labelMenuFornecedor" android:parentActivityName=".Main">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>

        <!-- Menu/MenuEstoque -->
        <activity android:name=".EstAddProduto" android:label="@string/labelEstAddProduto" android:parentActivityName=".MenuEstoque">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuEstoque" />
        </activity>
        <activity android:name=".EstVerEstoque" android:label="@string/labelEstVerProduto" android:parentActivityName=".MenuEstoque">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuEstoque" />
        </activity>
        <activity android:name=".EstReporEstoque" android:label="@string/labelEstRprProduto" android:parentActivityName=".MenuEstoque">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuEstoque" />
        </activity>
        <activity android:name=".EstBaixarEstoque" android:label="@string/labelEstBxrProduto" android:parentActivityName=".MenuEstoque">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuEstoque" />
        </activity>
        <activity android:name=".EstDelProduto" android:label="@string/labelEstDelProduto" android:parentActivityName=".MenuEstoque">
            <!-- Compatiilidade com a API level 7+ -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuEstoque" />
        </activity>
    </application>

</manifest>